<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Extension installer (Phase 4B): allows installing external Aniyomi-compatible APKs. -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- The install service shows a foreground notification (Android 13+ needs this). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Needed to receive PACKAGE_ADDED/REPLACED/REMOVED broadcasts on Android 14+. -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <application
        android:name=".App"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:allowBackup="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.ANIKUTA">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden|uiMode"
            android:theme="@style/Theme.ANIKUTA"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- ── Agent 2: Profile + Trackers — OAuth callback intent-filters ──
                 We use Aniyomi's Client IDs (5338 for AniList, 686b980... for MAL),
                 so the OAuth providers redirect to aniyomi:// URIs (configured on
                 their developer portals). We must catch these redirects so our app
                 appears in the "open with" list. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="aniyomi" android:host="anilist-auth" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="aniyomi" android:host="myanimelist-auth" />
            </intent-filter>
        </activity>

        <!--
            ErrorActivity — shown by AnikutaCrashHandler when an uncaught
            exception crashes the app. Shows the crash log + Copy / Restart /
            Close buttons. Not exported (internal only). Not the launcher.
        -->
        <activity
            android:name=".error.ErrorActivity"
            android:exported="false"
            android:theme="@style/Theme.ANIKUTA"
            android:taskAffinity=""
            android:excludeFromRecents="true" />

        <!-- Extension install service: foreground service hosting the PackageInstaller queue. -->
        <service
            android:name="app.confused.anikuta.data.extension.installer.ExtensionInstallService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!--
            ExtensionInstallReceiver is registered dynamically by AnimeExtensionManager
            (it needs a Listener constructor arg), so it is NOT declared here.
            The manager registers it for ACTION_PACKAGE_ADDED/REPLACED/REMOVED at startup.
        -->
    </application>
</manifest>
