# Session handoff — 2025-01-01 00:04 UTC

**Agent:** Z.ai Code (session 5 — reorg)
**Task ID:** phase-0-rules-docs-reorg
**Session started:** 2025-01-01 00:04 UTC
**Session ended:**   2025-01-01 00:04 UTC

## Goal of this session

Reorganize the repo's top-level documentation folders: rename `docs/` → `DOCS/`
and `rules/` → `RULES/` (uppercase, to match the convention already used for
`ANIYOMI_REFRENCE/`, `ANIKUTA_PROJECT/`, `OLD_ANIKUTA/`), create an
`ARCHITECTURE.md` stub at the repo root as the single source of truth, and fix
every cross-reference pointing at the old lowercase folder names.

## What I did

- **Folder rename (already on disk before this session):** old `docs/` → `DOCS/`,
  old `rules/` → `RULES/`. The new folders existed on disk; the old ones were
  already deleted.
- **Root-level files (already updated before this session):** `README.md`,
  `AGENTS.md`, `ARCHITECTURE.md` (new stub), `.github/CODEOWNERS`,
  `.github/workflows/ci-placeholder.yml`.
- **Updated all remaining cross-references** in 22 files (the 21 listed in the
  task + `DOCS/README.md` whose `# docs/` heading was stale):
  - 7 files under `ANIYOMI_REFRENCE/DOCUMENTATION/` (README, DI doc, build-system
    doc, compose-migration, 08-guides/README, how-to-add-features, troubleshooting).
    These are normally read-only (ADR-005), but were updated here because broken
    `docs/` paths would have made the reference docs point at non-existent files.
  - 2 files under `ANIKUTA_PROJECT/` (README, ANIKUTA/README).
  - 7 files under `DOCS/` (README + 01..06; only 04, 05, 01, README had refs).
  - 6 files under `RULES/` (notifications, session-handoff-template, 4 session
    notes 0000–0003).
- **Verification:** ran a repo-wide grep for `https://.*(DOCS|RULES)/` (corrupted
  URLs) → 0 matches. Ran a grep for leftover local `(docs|rules)/` path refs →
  only matches are inside `OLD_ANIKUTA/ANIKUTA_OLD/` (frozen reference, correctly
  untouched per ADR-007) and one conceptual phrase "docs/rules for AI agents" in
  `DOCS/04-design-decisions.md` ADR-001 context (not a path — left as-is).
- Wrote this handoff note.

## What I changed

- `ANIYOMI_REFRENCE/README.md` — 1 ref.
- `ANIYOMI_REFRENCE/DOCUMENTATION/01-architecture/02-dependency-injection.md` — 2 refs.
- `ANIYOMI_REFRENCE/DOCUMENTATION/00-overview/04-build-system.md` — 2 refs.
- `ANIYOMI_REFRENCE/DOCUMENTATION/06-ui/compose-migration.md` — 1 ref.
- `ANIYOMI_REFRENCE/DOCUMENTATION/08-guides/README.md` — 1 ref.
- `ANIYOMI_REFRENCE/DOCUMENTATION/08-guides/how-to-add-features.md` — 2 refs.
- `ANIYOMI_REFRENCE/DOCUMENTATION/08-guides/troubleshooting.md` — 4 refs
  (3 path refs + 1 "DOCS/RULES files" description).
- `ANIKUTA_PROJECT/README.md` — 3 refs (incl. bare `../docs/`).
- `ANIKUTA_PROJECT/ANIKUTA/README.md` — 8 refs (incl. `../../rules/sessions/`).
- `DOCS/README.md` — heading `# docs/` → `# DOCS/`.
- `DOCS/01-project-overview.md` — 1 ref (`../rules/` → `../RULES/`).
- `DOCS/04-design-decisions.md` — 6 refs (ADR-001, ADR-006, ADR-008 blocks).
- `DOCS/05-roadmap.md` — 4 refs (Phase 0 checklist + per-phase doc note).
- `DOCS/02-target-architecture.md`, `DOCS/03-reference-module-map.md`,
  `DOCS/06-build-and-ci.md` — no local `docs/`/`rules/` path refs (verified, no
  edits needed).
- `RULES/notifications.md` — 1 ref (`rules/sessions/` → `RULES/sessions/`).
- `RULES/session-handoff-template.md` — 1 ref (in HTML comment).
- `RULES/sessions/2025-01-01-0000-initial-setup.md` — 9 refs.
- `RULES/sessions/2025-01-01-0001-add-old-anikuta-reference.md` — 8 refs.
- `RULES/sessions/2025-01-01-0002-aniyomi-documentation.md` — 1 ref.
- `RULES/sessions/2025-01-01-0003-audit-verification.md` — 3 refs (incl. the
  historical "wrong-depth `../../../../docs/`" note in the audit-verification
  record, updated to `DOCS/` for consistency).
- `RULES/sessions/2025-01-01-0004-rules-reorg.md` — this file (new).

## Decisions made (and where they're recorded)

- **Folder casing convention.** Top-level project folders are now consistently
  UPPERCASE: `ANIYOMI_REFRENCE/`, `OLD_ANIKUTA/`, `ANIKUTA_PROJECT/`, `DOCS/`,
  `RULES/`. This makes the "our stuff" folders visually distinct from
  lowercase-nested folders like `.github/` and per-module `app/`, `data/`, etc.
  Not recorded as a new ADR — it is a cosmetic reorg of folders already
  established by ADR-001 / ADR-006.
- **Read-only reference exception.** `ANIYOMI_REFRENCE/DOCUMENTATION/` is normally
  frozen (ADR-005), but 7 of its files were updated here purely to fix
  `docs/` → `DOCS/` path references that would otherwise be broken. No prose
  content was changed — only the directory-name portion of local paths. External
  URLs (`https://aniyomi.org/...`, `https://anansi-project.org/docs/...`) were
  left untouched.

## What is DONE (exit criteria met)

- `DOCS/` and `RULES/` folders exist on disk (uppercase).
- Old `docs/` and `rules/` folders are deleted.
- `ARCHITECTURE.md` stub exists at repo root (created before this session).
- Root files (`README.md`, `AGENTS.md`, `ARCHITECTURE.md`, `.github/CODEOWNERS`,
  `.github/workflows/ci-placeholder.yml`) point at the new uppercase names.
- All 22 files with cross-references now use `DOCS/` and `RULES/`.
- Verified: 0 corrupted external URLs (`https://...DOCS/` / `https://...RULES/`).
- Verified: 0 remaining lowercase `docs/` / `rules/` path references in our
  editable files (the only remaining lowercase refs are inside the frozen
  `OLD_ANIKUTA/` snapshot, which is correct).

## What is NOT done / in progress

- Nothing from this task. The reorg is complete.
- (Pre-existing, not this session's scope) The `ARCHITECTURE.md` stub is still a
  stub — to be filled in during Phase 0b design.

## Blockers / open questions for the human

- None. The reorg was mechanical and is fully verified.

## What the NEXT agent should do

1. Confirm the reorg by spot-checking a couple of links (e.g. open
   `ANIKUTA_PROJECT/README.md` and click `../DOCS/02-target-architecture.md` —
   should resolve).
2. Proceed to **Phase 0b (Design & decisions)** per `DOCS/05-roadmap.md` — this is
   the same next step as before the reorg; nothing about the reorg changes the
   roadmap.
3. If writing new docs/rules, use `DOCS/` and `RULES/` (uppercase) in all path
   references.

## Pointers (files to read first)

- `ARCHITECTURE.md` — the new single source of truth (stub for now).
- `AGENTS.md` — already updated with the new folder names + read order.
- `DOCS/04-design-decisions.md` — ADR log (ADR-001 / ADR-006 / ADR-008 reference
  the folder layout).
- `DOCS/05-roadmap.md` — current phase + what's next.
- This file (`RULES/sessions/2025-01-01-0004-rules-reorg.md`) — what changed in
  the reorg.

## Dev environment notes

- Working directory: `/home/z/ani_kuta_workspace/ANI_KUTA_NEW`.
- No commits were made — file edits only, per task instructions.
- No builds performed (CI-only policy, ADR-003).
- The `OLD_ANIKUTA/ANIKUTA_OLD/` snapshot still contains lowercase `docs/` and
  `MEMORY/SESSION-LOGS/` references — these describe the *old project's own*
  folder layout (which it had renamed to `DOCS/` mid-life; see its session-1
  log). They are frozen and intentionally NOT updated.
