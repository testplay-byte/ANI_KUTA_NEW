# Session handoff — initial setup (Phase 0)

> This is the **initial setup session** that created the repository foundation
> (Phase 0). Read it to understand how the repo was bootstrapped.

**Agent:** initial-setup (Z.ai Code)
**Task ID:** setup-phase-0
**Session goal:** Set up the ANI_KUTA_NEW GitHub repository: structure it, import
the Aniyomi reference, write documentation & agent rules, configure CI, push.

## What I did

- Verified GitHub token + repo access (`testplay-byte/ANI_KUTA_NEW`, empty, public, default branch `main`).
- Cloned the empty repo into a workspace at `/home/z/ani_kuta_workspace/ANI_KUTA_NEW`.
- Configured a project-local git identity (`ani-kuta-bot`).
- Downloaded the Aniyomi source tarball (`archive/refs/heads/main.tar.gz`, source only, no `.git`).
- Extracted into `ANIYOMI_REFRENCE/ANIYOMI/` (24 MB, 2002 files).
- Created `ANIKUTA_PROJECT/ANIKUTA/` as a documented skeleton (no code yet).
- Wrote root `README.md` + `AGENTS.md`.
- Wrote `docs/01..06` (overview, target architecture draft, reference module map, ADR log, roadmap, build/CI).
- Wrote `rules/` (README, agent conventions, session handoff template).
- Added root `.gitignore`, `.github/CODEOWNERS`, `.github/workflows/ci-placeholder.yml`.
- Copied Aniyomi's Apache-2.0 `LICENSE` to root (ADR-004).
- Configured repo settings via GitHub API (description, homepage, topics).

## What is DONE (Phase 0 exit criteria)

- Repository structured and navigable.
- Aniyomi reference committed.
- Documentation + rules in place.
- CI placeholder workflow present.
- Build policy (CI-only) documented and enforced by convention.

## What is NOT done

- No ANIKUTA app code yet (intentional — that's Phase 1, after Phase 0b design).
- The CI workflow only runs a sanity job; no real build yet (nothing to build).
- Architecture not finalized (open questions in `docs/04-design-decisions.md`).

## What the NEXT agent should do

1. Begin **Phase 0b (Design & decisions)** in `docs/05-roadmap.md`.
2. Walk through every "open decision" in `docs/04-design-decisions.md` with the
   human owner and resolve each, adding new ADRs.
3. Finalize `docs/02-target-architecture.md`.
4. Only after that, proceed to Phase 1 scaffolding.

## Pointers (files to read first)

- `/README.md`
- `/AGENTS.md`
- `/docs/05-roadmap.md`
- `/docs/04-design-decisions.md`
- `/docs/02-target-architecture.md`

## Dev environment notes

- Working directory for this setup: `/home/z/ani_kuta_workspace/ANI_KUTA_NEW`.
- The Next.js sandbox at `/home/z/my-project` is unrelated to this task.
- GitHub remote uses an embedded fine-grained PAT in the clone URL (do not leak).
- Repo is currently **public**. Confirm with owner whether it should be private.
